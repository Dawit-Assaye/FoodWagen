{
  "buildCommand": "corepack enable && corepack install -g yarn@4.5.0 && yarn install && yarn turbo run build --filter=web",
  "installCommand": "corepack enable && corepack install -g yarn@4.5.0 && yarn install",
  "framework": null,
  "outputDirectory": "apps/web/.next"
}


{
  "name": "@foodwagen/ui",
  "version": "0.0.1",
  "types": "./src/index.ts",
  "main": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "sideEffects": false,
  "scripts": {
    "build": "tsup && tsc --project tsconfig.json --emitDeclarationOnly --outDir dist",
    "clean": "rm -rf dist",
    "lint": "eslint \"src/**/*.{ts,tsx}\"",
    "test": "vitest",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@foodwagen/eslint-config": "workspace:*",
    "@foodwagen/tailwind-config": "workspace:*",
    "@foodwagen/ts-config": "workspace:*",
    "tsup": "^8.5.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "tailwind-merge": "^2.5.2"
  }
}
